{
    "$schema": "https://railway.app/standard-schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:${PORT:-8080}",
        "restartPolicyType": "ON_FAILURE"
    }
}